const routes = {
    login: '/',
    register: '/register',
    messages: '/messages',
    explore: '/explore',
    profile: '/:nickname/*',
    albums: '*',
    tagged: 'tagged',
    home: '/',
};

export default routes;
