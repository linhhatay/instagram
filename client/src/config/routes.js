const routes = {
    login: '/',
    register: '/register',
    messages: '/messages',
    explore: '/explore',
    settings: '/settings',
    albums: '*',
    tagged: 'tagged',
    home: '/',
    profile: '/:nickname/*',
};

export default routes;
